"
Calculate Fib Program
Created Fri Aug 23, 2024
"

func int realFib: int n {
    if n <= 1 {
        return n ~
    } else {
        return fib.(n - 1) + fib.(n - 2) ~
    }
}

func int fib: int n {
    return realFib.(n) ~
}

func null @main: str[] args {
    println fib.(30).@str.() ~
}
