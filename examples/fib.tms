"
Calculate Fib Program
Created Fri Aug 23, 2024
"

struct A {
    let int p ~

    func int @new: {
        updt @this.p = 0 ~
        return 0 ~
    }
}

func int fib: int n {
    if n <= 1 {
        return n ~
    } else {
        return fib.(n - 1) + fib.(n - 2) ~
    }
}

func null @main: str[] args {
    println "Enter a positive integer." ~
    let str n = @readln.() ~
    let int p = n.@int.() ~
    println fib.(p).@str.() ~
}