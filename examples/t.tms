"
Testing file for syntax and development
Created 2/8/2025
"

struct B {
}

struct A {
    let B b ~

    func null @new: B b {
        updt @this.b = b ~
    }
}

func null @main: str[] args {
    let A a = $($() B) A ~
}

