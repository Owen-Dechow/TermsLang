class Loopy: {
    let int times_run;

    func null @new: {
        let int times_run = 0;
    }

    func null @loop: int n {
        loop i: i < n {
            cll i + i;
        }

        updt self.times_run += 1;
        print "Times Run: ";
        println self.times_run;
    }
}

func null main: str[] args {
    let Loopy loopy = $() Loopy;
    loop i: i < 10 {
        cll loopy.@loop.(10000000);
    }
}